import CountryList from "../components/CountryList";
import Inputs from "../components/Inputs";

const Home = () => {
  return (
    <main>
      <Inputs />
      <CountryList />
    </main>
  );
};

export default Home;
